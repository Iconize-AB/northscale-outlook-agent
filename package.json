{
  "name": "outlook-calendar-mcp",
  "version": "1.0.0",
  "description": "MCP server for Outlook Calendar integration via Microsoft Graph API",
  "type": "module",
  "main": "api/mcp.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@azure/msal-node": "^2.16.2",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=18"
  }
}
